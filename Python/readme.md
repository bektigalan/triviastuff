make unlimited virtual env
```
python -m venv {env-name}
```
On Windows, run:

```
{env-name}\Scripts\activate.bat
```
On Unix or MacOS, run:

```
source {env-name}/bin/activate
```
To deactivate a virtual environment, type:
```
deactivate
```